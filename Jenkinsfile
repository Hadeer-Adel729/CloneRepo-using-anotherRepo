pipeline {
    agent any
    stages {
        stage('Execute dir command') {
            steps {
                script {
                    bat 'execute_ls.sh'
                }
            }
        }
    }
}
