pipeline {
    agent any
    stages {
        stage('Execute dir command') {
            steps {
                script {
                    sh 'execute_dir.sh'
                }
            }
        }
    }
}
