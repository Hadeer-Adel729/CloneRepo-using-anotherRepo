pipeline {
    agent any    
    stages {
        stage('Execute ls command') {
            steps {
                script {
                    sh 'execute_ls.sh'
                }
            }
        }
    }
}
