pipeline {
   agent any
    stages {
        stage('Clone Repo from another Repo') {
           stage('Execute ls command') {
               steps {
                   script {
                       bat 'execute_ls.bat'
                   }
               }
           }
       }
   }
}
   
